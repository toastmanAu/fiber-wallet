# Fiber RPC Method Map

Generated from `references/fiber/crates/fiber-lib/src/rpc/README.md`.

| Scope | Module | Method | Biscuit Rule | Params | Returns |
| --- | --- | --- | --- | --- | --- |
| Post-MVP | Cch | `send_btc` | `allow if write("cch");` | * `btc_pay_req` - `String`, Payment request string for the BTC Lightning payee.<br>* `currency` - Currency, Request currency | * `timestamp` - `u64`, Seconds since epoch when the order is created<br>* `expiry_delta_seconds` - `u64`, Relative expiry time in seconds from `created_at` that the order expires<br>* `wrapped_btc_type_script` - `Script`, Wrapped BTC type script<br>* `incoming_invoice` - CchInvoice, Generated invoice for the incoming payment<br>* `outgoing_pay_req` - `String`, The final payee to accept the payment. It has the different network with incoming invoice.<br>* `payment_hash` - Hash256, Payment hash for the HTLC for both CKB and BTC.<br>* `amount_sats` - `u128`, Amount required to pay in Satoshis, including fee<br>* `fee_sats` - `u128`, Fee in Satoshis<br>* `status` - CchOrderStatus, Order status |
| Post-MVP | Cch | `receive_btc` | `allow if read("cch");` | * `fiber_pay_req` - `String`, Payment request string for the CKB Fiber payee. | * `timestamp` - `u64`, Seconds since epoch when the order is created<br>* `expiry_delta_seconds` - `u64`, Relative expiry time in seconds from `created_at` that the order expires<br>* `wrapped_btc_type_script` - `Script`, Wrapped BTC type script<br>* `incoming_invoice` - CchInvoice, Generated invoice for the incoming payment<br>* `outgoing_pay_req` - `String`, The final payee to accept the payment. It has the different network with incoming invoice.<br>* `payment_hash` - Hash256, Payment hash for the HTLC for both CKB and BTC.<br>* `amount_sats` - `u128`, Amount required to pay in Satoshis, including fee<br>* `fee_sats` - `u128`, Fee in Satoshis<br>* `status` - CchOrderStatus, Order status |
| Post-MVP | Cch | `get_cch_order` | `allow if read("cch");` | * `payment_hash` - Hash256, Payment hash for the HTLC for both CKB and BTC. | * `timestamp` - `u64`, Seconds since epoch when the order is created<br>* `expiry_delta_seconds` - `u64`, Relative expiry time in seconds from `created_at` that the order expires<br>* `wrapped_btc_type_script` - `Script`, Wrapped BTC type script<br>* `incoming_invoice` - CchInvoice, Generated invoice for the incoming payment<br>* `outgoing_pay_req` - `String`, The final payee to accept the payment. It has the different network with incoming invoice.<br>* `payment_hash` - Hash256, Payment hash for the HTLC for both CKB and BTC.<br>* `amount_sats` - `u128`, Amount required to pay in Satoshis, including fee<br>* `fee_sats` - `u128`, Fee in Satoshis<br>* `status` - CchOrderStatus, Order status |
| MVP | Channel | `open_channel` | `allow if write("channels");` | * `pubkey` - Pubkey, The public key of the peer to open a channel with.<br>* `funding_amount` - `u128`, The amount of CKB or UDT to fund the channel with.<br>* `public` - `Option<bool>`, Whether this is a public channel (will be broadcasted to network, and can be used to forward TLCs),<br>* `one_way` - `Option<bool>`, Whether this is a one-way channel (will not be broadcasted to network, and can only be used to send payment one way),<br>* `funding_udt_type_script` - `Option<Script>`, The type script of the UDT to fund the channel with, an optional parameter.<br>* `shutdown_script` - `Option<Script>`, The script used to receive the channel balance, an optional parameter,<br>* `commitment_delay_epoch` - `Option<EpochNumberWithFraction>`, The delay time for the commitment transaction, must be an<br>* `commitment_fee_rate` - `Option<u64>`, The fee rate for the commitment transaction, an optional parameter.<br>* `funding_fee_rate` - `Option<u64>`, The fee rate for the funding transaction, an optional parameter.<br>* `tlc_expiry_delta` - `Option<u64>`, The expiry delta to forward a tlc, in milliseconds, default to 4 hours, which is 4 * 60 * 60 * 1000 milliseconds<br>* `tlc_min_value` - `Option<u128>`, The minimum value for a TLC our side can send,<br>* `tlc_fee_proportional_millionths` - `Option<u128>`, The fee proportional millionths for a TLC, proportional to the amount of the forwarded tlc.<br>* `max_tlc_value_in_flight` - `Option<u128>`, The maximum total value of in-flight TLCs our side will accept from the peer, an optional parameter.<br>* `max_tlc_number_in_flight` - `Option<u64>`, The maximum number of in-flight TLCs our side will accept from the peer, an optional parameter, default is 125 | * `temporary_channel_id` - Hash256, The temporary channel ID of the channel being opened |
| MVP | Channel | `accept_channel` | `allow if write("channels");` | * `temporary_channel_id` - Hash256, The temporary channel ID of the channel to accept<br>* `funding_amount` - `u128`, The amount of CKB or UDT to fund the channel with<br>* `shutdown_script` - `Option<Script>`, The script used to receive the channel balance, an optional parameter,<br>* `max_tlc_value_in_flight` - `Option<u128>`, The maximum total value of in-flight TLCs our side will accept from the peer, default is u128::MAX<br>* `max_tlc_number_in_flight` - `Option<u64>`, The maximum number of in-flight TLCs our side will accept from the peer, default is 125<br>* `tlc_min_value` - `Option<u128>`, The minimum value for a TLC our side can send,<br>* `tlc_fee_proportional_millionths` - `Option<u128>`, The fee proportional millionths for a TLC, proportional to the amount of the forwarded tlc.<br>* `tlc_expiry_delta` - `Option<u64>`, The expiry delta to forward a tlc, in milliseconds, default to 1 day, which is 24 * 60 * 60 * 1000 milliseconds | * `channel_id` - Hash256, The final ID of the channel that was accepted, it's different from the temporary channel ID |
| Post-MVP | Channel | `abandon_channel` | `allow if write("channels");` | * `channel_id` - Hash256, The temporary channel ID or real channel ID of the channel being abandoned | none |
| MVP | Channel | `list_channels` | `allow if read("channels");` | * `pubkey` - Option<Pubkey>, The public key to list channels for.<br>* `include_closed` - `Option<bool>`, Whether to include closed channels in the list, an optional parameter, default value is false<br>* `only_pending` - `Option<bool>`, When set to true, only return channels that are still being opened (non-final states: | * `channels` - Vec<Channel>, The list of channels |
| MVP | Channel | `shutdown_channel` | `allow if write("channels");` | * `channel_id` - Hash256, The channel ID of the channel to shut down<br>* `close_script` - `Option<Script>`, The script used to receive the channel balance, only support secp256k1_blake160_sighash_all script for now<br>* `fee_rate` - `Option<u64>`, The fee rate for the closing transaction, the fee will be deducted from the closing initiator's channel balance<br>* `force` - `Option<bool>`, Whether to force the channel to close, when set to false, `close_script` and `fee_rate` should be set, default is false. | none |
| MVP | Channel | `update_channel` | `allow if write("channels");` | * `channel_id` - Hash256, The channel ID of the channel to update<br>* `enabled` - `Option<bool>`, Whether the channel is enabled, default value is true<br>* `tlc_expiry_delta` - `Option<u64>`, The expiry delta for the TLC locktime<br>* `tlc_minimum_value` - `Option<u128>`, The minimum value for a TLC<br>* `tlc_fee_proportional_millionths` - `Option<u128>`, The fee proportional millionths for a TLC | none |
| Post-MVP | Channel | `open_channel_with_external_funding` | `allow if write("channels");` | * `pubkey` - Pubkey, The identity public key of the peer to open a channel with.<br>* `funding_amount` - `u128`, The amount of CKB or UDT to fund the channel with.<br>* `public` - `Option<bool>`, Whether this is a public channel (will be broadcasted to network, and can be used to forward TLCs), an optional parameter, default value is true.<br>* `funding_udt_type_script` - `Option<Script>`, The type script of the UDT to fund the channel with, an optional parameter.<br>* `shutdown_script` - `Script`, The script used to receive the channel balance when the channel is closed. This is REQUIRED for external funding.<br>* `funding_lock_script` - `Script`, The lock script that controls the funding cells. The node will collect cells with this lock script<br>* `funding_lock_script_cell_deps` - `Option<Vec<CellDep>>`, Optional extra cell deps required by `funding_lock_script`.<br>* `commitment_delay_epoch` - `Option<EpochNumberWithFraction>`, The delay time for the commitment transaction, must be an<br>* `commitment_fee_rate` - `Option<u64>`, The fee rate for the commitment transaction, an optional parameter.<br>* `funding_fee_rate` - `Option<u64>`, The fee rate for the funding transaction, an optional parameter.<br>* `tlc_expiry_delta` - `Option<u64>`, The expiry delta to forward a tlc, in milliseconds, default to 4 hours, which is 4 * 60 * 60 * 1000 milliseconds<br>* `tlc_min_value` - `Option<u128>`, The minimum value for a TLC our side can send,<br>* `tlc_fee_proportional_millionths` - `Option<u128>`, The fee proportional millionths for a TLC, proportional to the amount of the forwarded tlc.<br>* `max_tlc_value_in_flight` - `Option<u128>`, The maximum total value of in-flight TLCs our side will accept from the peer, an optional parameter.<br>* `max_tlc_number_in_flight` - `Option<u64>`, The maximum number of in-flight TLCs our side will accept from the peer, an optional parameter, default is 125 | * `channel_id` - Hash256, The channel ID of the channel being opened.<br>* `unsigned_funding_tx` - `Transaction`, The final unsigned funding transaction that needs to be signed. |
| Post-MVP | Channel | `submit_signed_funding_tx` | `allow if write("channels");` | * `channel_id` - Hash256, The channel ID returned from `open_channel_with_external_funding`.<br>* `signed_funding_tx` - `Transaction`, The signed funding transaction. | * `channel_id` - Hash256, The channel ID.<br>* `funding_tx_hash` - Hash256, The hash of the funding transaction that was submitted. |
| Post-MVP | Dev | `commitment_signed` | `allow if write("messages");` | * `channel_id` - Hash256, The channel ID of the channel to send the commitment_signed message to | none |
| Post-MVP | Dev | `add_tlc` | `allow if write("channels");` | * `channel_id` - Hash256, The channel ID of the channel to add the TLC to<br>* `amount` - `u128`, The amount of the TLC<br>* `payment_hash` - Hash256, The payment hash of the TLC<br>* `expiry` - `u64`, The expiry of the TLC<br>* `hash_algorithm` - Option<HashAlgorithm>, The hash algorithm of the TLC | * `tlc_id` - `u64`, The ID of the TLC |
| Post-MVP | Dev | `remove_tlc` | `allow if write("channels");` | * `channel_id` - Hash256, The channel ID of the channel to remove the TLC from<br>* `tlc_id` - `u64`, The ID of the TLC to remove<br>* `reason` - RemoveTlcReason, The reason for removing the TLC, either a 32-byte hash for preimage fulfillment or an u32 error code for removal | none |
| Post-MVP | Dev | `submit_commitment_transaction` | `allow if write("chain");` | * `channel_id` - Hash256, Channel ID<br>* `commitment_number` - `u64`, Commitment number | * `tx_hash` - Hash256, Submitted commitment transaction hash |
| Post-MVP | Dev | `check_channel_shutdown` | `allow if write("channels");` | * `channel_id` - Hash256, Channel ID | none |
| Post-MVP | Dev | `sign_external_funding_tx` | `allow if write("channels");` | * `unsigned_funding_tx` - `ckb_jsonrpc_types::Transaction`, The unsigned funding transaction returned from `open_channel_with_external_funding`.<br>* `private_key` - `String`, The private key to sign the transaction, as a 0x-prefixed 32-byte hex string. | * `signed_funding_tx` - `ckb_jsonrpc_types::Transaction`, The signed funding transaction that can be submitted via `submit_signed_funding_tx`. |
| MVP | Graph | `graph_nodes` | `allow if read("graph");` | * `limit` - `Option<u64>`, The maximum number of nodes to return.<br>* `after` - `Option<JsonBytes>`, The cursor to start returning nodes from. | * `nodes` - Vec<NodeInfo>, The list of nodes.<br>* `last_cursor` - `JsonBytes`, The last cursor. |
| MVP | Graph | `graph_channels` | `allow if read("graph");` | * `limit` - `Option<u64>`, The maximum number of channels to return.<br>* `after` - `Option<JsonBytes>`, The cursor to start returning channels from. | * `channels` - Vec<ChannelInfo>, A list of channels.<br>* `last_cursor` - `JsonBytes`, The last cursor for pagination. |
| MVP | Info | `node_info` | `allow if read("node");` | none | * `version` - `String`, The version of the node software.<br>* `commit_hash` - `String`, The commit hash of the node software.<br>* `pubkey` - Pubkey, The identity public key of this node (secp256k1 compressed, hex without 0x prefix).<br>* `features` - `Vec<String>`, The features supported by the node.<br>* `node_name` - `Option<String>`, The optional name of the node.<br>* `addresses` - `Vec<String>`, A list of multi-addresses associated with the node (as strings).<br>* `chain_hash` - Hash256, The hash of the blockchain that the node is connected to.<br>* `open_channel_auto_accept_min_ckb_funding_amount` - `u64`, The minimum CKB funding amount for automatically accepting open channel requests, serialized as a hexadecimal string.<br>* `auto_accept_channel_ckb_funding_amount` - `u64`, The CKB funding amount for automatically accepting channel requests, serialized as a hexadecimal string.<br>* `default_funding_lock_script` - `Script`, The default funding lock script for the node.<br>* `tlc_expiry_delta` - `u64`, The locktime expiry delta for Time-Locked Contracts (TLC), serialized as a hexadecimal string.<br>* `tlc_min_value` - `u128`, The minimum value for Time-Locked Contracts (TLC) we can send, serialized as a hexadecimal string.<br>* `tlc_fee_proportional_millionths` - `u128`, The fee (to forward payments) proportional to the value of Time-Locked Contracts (TLC),<br>* `channel_count` - `u32`, The number of channels associated with the node, serialized as a hexadecimal string.<br>* `pending_channel_count` - `u32`, The number of pending channels associated with the node, serialized as a hexadecimal string.<br>* `peers_count` - `u32`, The number of peers connected to the node, serialized as a hexadecimal string.<br>* `udt_cfg_infos` - UdtCfgInfos, Configuration information for User-Defined Tokens (UDT) associated with the node. |
| MVP | Invoice | `new_invoice` | `allow if write("invoices");` | * `amount` - `u128`, The amount of the invoice.<br>* `description` - `Option<String>`, The description of the invoice.<br>* `currency` - Currency, The currency of the invoice.<br>* `payment_preimage` - Option<Hash256>, The preimage to settle an incoming TLC payable to this invoice. If preimage is set, hash must be absent.<br>* `payment_hash` - Option<Hash256>, The hash of the preimage. If hash is set, preimage must be absent. This condition indicates a 'hold invoice'<br>* `expiry` - `Option<u64>`, The expiry time of the invoice, in seconds.<br>* `fallback_address` - `Option<String>`, The fallback address of the invoice.<br>* `final_expiry_delta` - `Option<u64>`, The final HTLC timeout of the invoice, in milliseconds.<br>* `udt_type_script` - `Option<Script>`, The UDT type script of the invoice.<br>* `hash_algorithm` - Option<HashAlgorithm>, The hash algorithm of the invoice.<br>* `allow_mpp` - `Option<bool>`, Whether allow payment to use MPP<br>* `allow_trampoline_routing` - `Option<bool>`, Whether allow payment to use trampoline routing | * `invoice_address` - `String`, The encoded invoice address.<br>* `invoice` - CkbInvoice, The invoice. |
| MVP | Invoice | `parse_invoice` | `allow if read("invoices");` | * `invoice` - `String`, The encoded invoice address. | * `invoice` - CkbInvoice, The invoice. |
| MVP | Invoice | `get_invoice` | `allow if read("invoices");` | * `payment_hash` - Hash256, The payment hash of the invoice. | * `invoice_address` - `String`, The encoded invoice address.<br>* `invoice` - CkbInvoice, The invoice.<br>* `status` - CkbInvoiceStatus, The invoice status |
| MVP | Invoice | `cancel_invoice` | `allow if write("invoices");` | * `payment_hash` - Hash256, The payment hash of the invoice. | * `invoice_address` - `String`, The encoded invoice address.<br>* `invoice` - CkbInvoice, The invoice.<br>* `status` - CkbInvoiceStatus, The invoice status |
| Post-MVP | Invoice | `settle_invoice` | `allow if write("invoices");` | * `payment_hash` - Hash256, The payment hash of the invoice.<br>* `payment_preimage` - Hash256, The payment preimage of the invoice. | none |
| MVP | Payment | `send_payment` | `allow if write("payments");` | * `target_pubkey` - Option<Pubkey>, The public key (`Pubkey`) of the payment target node, serialized as a hex string.<br>* `amount` - `Option<u128>`, the amount of the payment, the unit is Shannons for non UDT payment<br>* `payment_hash` - Option<Hash256>, the hash to use within the payment's HTLC.<br>* `final_tlc_expiry_delta` - `Option<u64>`, the TLC expiry delta should be used to set the timelock for the final hop, in milliseconds<br>* `tlc_expiry_limit` - `Option<u64>`, the TLC expiry limit for the whole payment, in milliseconds, each hop is with a default tlc delta of 1 day<br>* `invoice` - `Option<String>`, the encoded invoice to send to the recipient<br>* `timeout` - `Option<u64>`, the payment timeout in seconds, if the payment is not completed within this time, it will be cancelled<br>* `max_fee_amount` - `Option<u128>`, the maximum fee amounts in shannons that the sender is willing to pay.<br>* `max_fee_rate` - `Option<u64>`, the maximum fee rate per thousand, default is 5 (0.5%)<br>* `max_parts` - `Option<u64>`, max parts for the payment, only used for multi-part payments<br>* `trampoline_hops` - Option<Vec<Pubkey>>, Optional explicit trampoline hops.<br>* `keysend` - `Option<bool>`, keysend payment<br>* `udt_type_script` - `Option<Script>`, udt type script for the payment<br>* `allow_self_payment` - `Option<bool>`, Allow paying yourself through a circular route, default is false.<br>* `custom_records` - Option<PaymentCustomRecords>, Some custom records for the payment which contains a map of u32 to Vec<u8><br>* `hop_hints` - Option<Vec<HopHint>>, Optional route hints to reach the destination through private channels.<br>* `dry_run` - `Option<bool>`, dry_run for payment, used for check whether we can build valid router and the fee for this payment, | * `payment_hash` - Hash256, The payment hash of the payment<br>* `status` - PaymentStatus, The status of the payment<br>* `created_at` - `u64`, The time the payment was created at, in milliseconds from UNIX epoch<br>* `last_updated_at` - `u64`, The time the payment was last updated at, in milliseconds from UNIX epoch<br>* `failed_error` - `Option<String>`, The error message if the payment failed<br>* `fee` - `u128`, fee paid for the payment<br>* `custom_records` - Option<PaymentCustomRecords>, The custom records to be included in the payment.<br>* `routers` - Vec<SessionRoute>, The router is a list of nodes that the payment will go through. |
| MVP | Payment | `get_payment` | `allow if read("payments");` | * `payment_hash` - Hash256, The payment hash of the payment to retrieve | * `payment_hash` - Hash256, The payment hash of the payment<br>* `status` - PaymentStatus, The status of the payment<br>* `created_at` - `u64`, The time the payment was created at, in milliseconds from UNIX epoch<br>* `last_updated_at` - `u64`, The time the payment was last updated at, in milliseconds from UNIX epoch<br>* `failed_error` - `Option<String>`, The error message if the payment failed<br>* `fee` - `u128`, fee paid for the payment<br>* `custom_records` - Option<PaymentCustomRecords>, The custom records to be included in the payment.<br>* `routers` - Vec<SessionRoute>, The router is a list of nodes that the payment will go through. |
| MVP | Payment | `build_router` | `allow if read("payments");` | * `amount` - `Option<u128>`, the amount of the payment, the unit is Shannons for non UDT payment<br>* `udt_type_script` - `Option<Script>`, udt type script for the payment router<br>* `hops_info` - Vec<HopRequire>, A list of hops that defines the route. This does not include the source hop pubkey.<br>* `final_tlc_expiry_delta` - `Option<u64>`, the TLC expiry delta should be used to set the timelock for the final hop, in milliseconds | * `router_hops` - Vec<RouterHop>, The hops information for router |
| MVP | Payment | `send_payment_with_router` | `allow if write("payments");` | * `payment_hash` - Option<Hash256>, the hash to use within the payment's HTLC.<br>* `router` - Vec<RouterHop>, The router to use for the payment<br>* `invoice` - `Option<String>`, the encoded invoice to send to the recipient<br>* `custom_records` - Option<PaymentCustomRecords>, Some custom records for the payment which contains a map of u32 to Vec<u8><br>* `keysend` - `Option<bool>`, keysend payment<br>* `udt_type_script` - `Option<Script>`, udt type script for the payment<br>* `dry_run` - `Option<bool>`, dry_run for payment, used for check whether we can build valid router and the fee for this payment, | * `payment_hash` - Hash256, The payment hash of the payment<br>* `status` - PaymentStatus, The status of the payment<br>* `created_at` - `u64`, The time the payment was created at, in milliseconds from UNIX epoch<br>* `last_updated_at` - `u64`, The time the payment was last updated at, in milliseconds from UNIX epoch<br>* `failed_error` - `Option<String>`, The error message if the payment failed<br>* `fee` - `u128`, fee paid for the payment<br>* `custom_records` - Option<PaymentCustomRecords>, The custom records to be included in the payment.<br>* `routers` - Vec<SessionRoute>, The router is a list of nodes that the payment will go through. |
| MVP | Payment | `list_payments` | `allow if read("payments");` | * `status` - Option<PaymentStatus>, Filter payments by status. If not set, all payments are returned.<br>* `limit` - `Option<u64>`, The maximum number of payments to return. Default is 15.<br>* `after` - Option<Hash256>, The payment hash to start returning payments after (exclusive cursor for pagination). | * `payments` - Vec<GetPaymentCommandResult>, The list of payments.<br>* `last_cursor` - Option<Hash256>, The last cursor for pagination. Use this as `after` in the next request to get more results. |
| MVP | Peer | `connect_peer` | `allow if write("peers");` | * `address` - `Option<String>`, The address of the peer to connect to (as a multiaddr string).<br>* `pubkey` - Option<Pubkey>, The public key of the peer to connect to.<br>* `save` - `Option<bool>`, Whether to save the peer address to the peer store.<br>* `addr_type` - Option<TransportType>, Filter addresses by transport type when connecting by pubkey. | none |
| MVP | Peer | `disconnect_peer` | `allow if write("peers");` | * `pubkey` - Pubkey, The public key of the peer to disconnect. | none |
| MVP | Peer | `list_peers` | `allow if read("peers");` | none | * `peers` - Vec<PeerInfo>, A list of connected peers. |
| Post-MVP | Prof | `pprof` | `allow if write("pprof");` | * `duration_secs` - `Option<u64>`, Duration to profile in seconds. Defaults 10s. | * `path` - `String`, Path of the generated flamegraph SVG. |
| Post-MVP | Watchtower | `create_watch_channel` | `allow if write("watchtower");` | * `channel_id` - Hash256, Channel ID<br>* `funding_udt_type_script` - `Option<Script>`, Funding UDT type script<br>* `local_settlement_key` - Privkey, The local party's private key used to settle the commitment transaction (hex without 0x prefix)<br>* `remote_settlement_key` - Pubkey, The remote party's public key used to settle the commitment transaction (hex without 0x prefix)<br>* `local_funding_pubkey` - Pubkey, The local party's funding public key (hex without 0x prefix)<br>* `remote_funding_pubkey` - Pubkey, The remote party's funding public key (hex without 0x prefix)<br>* `settlement_data` - SettlementData, Settlement data | none |
| Post-MVP | Watchtower | `remove_watch_channel` | `allow if write("watchtower");` | * `channel_id` - Hash256, Channel ID | none |
| Post-MVP | Watchtower | `update_revocation` | `allow if write("watchtower");` | * `channel_id` - Hash256, Channel ID<br>* `revocation_data` - RevocationData, Revocation data<br>* `settlement_data` - SettlementData, Settlement data | none |
| Post-MVP | Watchtower | `update_pending_remote_settlement` | `allow if write("watchtower");` | * `channel_id` - Hash256, Channel ID<br>* `settlement_data` - SettlementData, Settlement data | none |
| Post-MVP | Watchtower | `update_local_settlement` | `allow if write("watchtower");` | * `channel_id` - Hash256, Channel ID<br>* `settlement_data` - SettlementData, Settlement data | none |
| Post-MVP | Watchtower | `create_preimage` | `allow if write("watchtower");` | * `payment_hash` - Hash256, Payment hash<br>* `preimage` - Hash256, Preimage | none |
| Post-MVP | Watchtower | `remove_preimage` | `allow if write("watchtower");` | * `payment_hash` - Hash256, Payment hash | none |
